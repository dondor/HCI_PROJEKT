using UnityEngine;
using System.Collections;

public class SniegSkrypt : MonoBehaviour {

	// Use this for initialization
	void Start () {
		renderer.sortingLayerName = "pierwszy plan";
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
